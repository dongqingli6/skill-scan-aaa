# Big Stage 30 Open Source Release Package

This module prepares sanitized GitHub and competition-submission materials. It reads existing results, generates documentation, builds public artifacts, and writes a release audit and manifest.

It does not execute skills, run Docker, run Codex or Claude Code, run strace, call APIs, enable network access, delete original files, or copy raw real skill archives into public artifacts.
